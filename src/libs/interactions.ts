export enum VoteDir {
    Up = 'up',
    Down = 'down',
    None = 'none'
}

export enum RateableType {
    Post = 'posts',
    Series = 'series',
    Question = 'questions',
    Answer = 'answers',
    Comment = 'comments'
}

export enum SubscribableType {
    Tag = 'tag',
    User = 'user',
    Post = 'post',
    Series = 'series',
    Question = 'question'
}

export enum CommentableType {
    Post = 'posts',
    Series = 'series',
    Question = 'questions',
    Answer = 'answers'
}
